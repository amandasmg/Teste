<?php
require_once("../../../config.php");
global $CFG;

// Função utilizada para modificar alguns ícones do Moodle sem interferir na pasta "moodle/pix/". 
?>

function mudarImagens() {
/*	
	var imgs = document.images;
	var i;
	for (i=0; i < imgs.length; i++) {
		if ((imgs[i].src).match("closed.gif")) imgs[i].src = '<?php echo $CFG->themewww.'/'.current_theme() ?>/pix/closed.gif';
		else if ((imgs[i].src).match("open.gif")) imgs[i].src = '<?php echo $CFG->themewww.'/'.current_theme() ?>/pix/open.gif';
	}
*/
}

